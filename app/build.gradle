apply plugin: 'com.android.application'

android {
    compileSdk 34

    defaultConfig {
        applicationId "mobile.application.forfree.earth"
        minSdkVersion 11
        targetSdkVersion 34
    }

    buildTypes {
        release {
            minifyEnabled true
            proguardFiles 'proguard.cfg'
        }
    }
    namespace 'mobile.application.forfree.earth'
}
